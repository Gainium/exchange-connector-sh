module.exports = {
  semi: false,
  trailingComma: 'all',
  singleQuote: true,
  printWidth: 80,
  tabWidth: 2,
  // Tolerate CRLF working trees (Windows contributors) instead of emitting a
  // `Delete ␍` prettier warning on every line of every file.
  endOfLine: 'auto',
}
